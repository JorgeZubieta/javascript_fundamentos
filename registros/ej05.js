// modificacion de registros, basandonos en un ejemplo

// Definí el procedimiento moverArchivo, que recibe un registro
// y una nueva ruta y modifica el archivo con la nueva ruta.

function moverArchivo(registro, nuevaRuta){
    registro.ruta = nuevaRuta;
}
// NOTA -> debi basarme en el ejemplo para darme cuenta de
// que debi poner ruta simplemente!